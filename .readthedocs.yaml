version: 2
build:
  os: "ubuntu-20.04"
  tools:
    python: "3.12"
  jobs:
    install:
      - pip install --upgrade pip
      - pip install --group 'docs'

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false
